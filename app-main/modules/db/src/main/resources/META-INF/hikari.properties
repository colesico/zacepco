# Base connection settings
jdbcUrl=jdbc:sqlite:data/zacepco.db

# Connection pool settings for SQLite
maximumPoolSize=2
minimumIdle=1

# Timeouts (in milliseconds)
connectionTimeout=5000
maxLifetime=1800000
idleTimeout=600000

# Pool name for logging purposes
poolName=SQLiteHikariPool
